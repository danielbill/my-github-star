---
phase: 01-user-settings
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [frontend/src/components/Settings.tsx, frontend/src/App.tsx, frontend/src/components/MainLayout.tsx]
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05]
user_setup: []

must_haves:
  truths:
    - "Settings icon visible in header left of login icon"
    - "Clicking settings icon switches to settings view"
    - "Settings page displays with '设置' title"
    - "User can adjust refresh interval using slider with preset marks"
    - "User can select GitHub clone directory via folder picker"
    - "Settings auto-save when modified (no manual save button)"
    - "User can return to main view from settings"
  artifacts:
    - path: "frontend/src/components/Settings.tsx"
      provides: "Settings page component with auto-save"
      min_lines: 100
    - path: "frontend/src/App.tsx"
      provides: "/settings route"
      min_lines: 200
    - path: "frontend/src/components/MainLayout.tsx"
      provides: "Settings icon and navigation"
      min_lines: 500
  key_links:
    - from: "frontend/src/components/Settings.tsx"
      to: "backend/app.go"
      via: "GetSettings and UpdateSettings Wails bindings"
      pattern: "import.*GetSettings|import.*UpdateSettings"
    - from: "frontend/src/components/MainLayout.tsx"
      to: "frontend/src/App.tsx"
      via: "onNavigate callback"
      pattern: "onNavigate.*settings"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/Settings.tsx"
      via: "Route /settings"
      pattern: "Route.*settings.*Settings"
---

<objective>
Create settings page UI with auto-save functionality and integrate with header navigation.

Purpose: Provide user interface for configuring refresh interval and GitHub clone directory with seamless persistence.

Output: Settings page component with Slider for refresh interval and folder picker for clone directory, integrated with main header navigation.
</objective>

<execution_context>
@C:/Users/bi_da/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bi_da/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-user-settings/01-CONTEXT.md
@.planning/phases/01-user-settings/01-01-SUMMARY.md
@frontend/src/App.tsx
@frontend/src/components/MainLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Settings page component with auto-save</name>
  <files>frontend/src/components/Settings.tsx</files>
  <action>
    Create new component file Settings.tsx with:

    1. Imports: React, useState, useEffect from 'react'; Slider, TextInput, Button, Text, Title, Stack, Card from '@mantine/core'; IconSettings, IconFolder from '@tabler/icons-react'; GetSettings, UpdateSettings from '../../wailsjs/go/backend/App'

     2. State: refreshInterval (number, default 0.5), cloneDirectory (string, default ''), loading (boolean), error (string, default '')

    3. useEffect on mount: Call GetSettings() to load initial values, set state

    4. Layout: Card with padding, contains:
       - Title "设置" (Title order={3})
       - Stack spacing="md" with settings items:
         a. Refresh Interval section:
            - Label text: "刷新间隔"
            - Slider component with:
              * min: 0.1, max: 24, step: null
              * marks: [{value: 0.5, label: '30m'}, {value: 1, label: '1h'}, {value: 4, label: '4h'}, {value: 24, label: '24h'}]
              * restrictToMarks: true
              * value: refreshInterval
              * onChange: (value) => updateRefreshInterval(value)
         b. Clone Directory section:
            - Label text: "GitHub 克隆目录"
            - TextInput with:
              * value: cloneDirectory
              * rightSection: Button with IconFolder, label "浏览" (size="xs")
              * readOnly: true (only change via folder picker)
              * rightSection props: onClick: handleFolderPicker
         c. Back button (optional, for standalone use):
            - Button: "返回", onClick: handleBack

     5. Update functions:
        - updateRefreshInterval: 
          * Calls UpdateSettings({refresh_interval: value, github_clone_dir: cloneDirectory}) on change
          * On success: setError('')
          * On failure: setError(err instanceof Error ? err.message : '保存失败')
        - handleFolderPicker: 
          * Calls new backend method OpenDirectoryDialog (see Task 2)
          * If path selected: UpdateSettings({refresh_interval: refreshInterval, github_clone_dir: selectedPath})
          * On success: setError('')
          * On failure: setError(err instanceof Error ? err.message : '保存失败')

     6. Error handling: 
        - Add inline error display below Slider: {error && <Text c="red" size="sm">{error}</Text>}
        - Add inline error display below TextInput: {error && <Text c="red" size="sm">{error}</Text>}
        - On UpdateSettings failure, set error state and display inline (discretion: "Inline error messages below input fields")
        - Success notifications remain silent per user decision (no toast)

    Follow existing pattern: ProjectList.tsx shows component structure, use Mantine components with custom styles matching theme.
  </action>
  <verify>
    Run `npm run build` in frontend directory to verify compilation succeeds.
    Check that Settings.tsx exports Settings component.
  </verify>
   <done>
     Settings.tsx component created with refresh interval slider (preset marks, restrictToMarks).
     Clone directory input with folder picker button.
     Auto-save on value changes (UpdateSettings called).
     Error state tracking with inline error messages below inputs.
   </done>
</task>

<task type="auto">
  <name>Task 2: Add folder picker backend method and route</name>
  <files>backend/app.go, frontend/src/App.tsx</files>
  <action>
    backend/app.go:
    1. Add OpenDirectoryDialog() exported method that calls runtime.OpenDirectoryDialog with:
       - title: "选择 GitHub 克隆目录"
       - defaultDir: a.appConfig.GetGitHubCloneDir() if set, else os.UserHomeDir()
    2. Return the selected directory path or empty string on cancel

    frontend/src/App.tsx:
    1. Add Settings import: import { Settings } from './components/Settings'
    2. Add new route: <Route path="/settings" element={<Settings onNavigate={handleNavigate} />} />
    3. Add handleNavigate callback to App component (optional, for navigating back to main view):
       const handleNavigate = useCallback((path: string) => {
         // Could use navigate() from react-router if needed
       }, [])

    Follow existing pattern: OpenURL() method (line 162-167) shows runtime usage for dialogs.
  </action>
  <verify>
    Run `wails dev` to regenerate TypeScript bindings.
    Check that OpenDirectoryDialog is exported in wailsjs bindings.
    Visit /settings route directly and verify Settings component renders.
  </verify>
  <done>
    OpenDirectoryDialog backend method exists.
    /settings route added to App.tsx.
    Settings component accessible via route.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add settings icon and navigation to header</name>
  <files>frontend/src/components/MainLayout.tsx</files>
  <action>
    1. Import IconSettings from '@tabler/icons-react'

    2. Add onNavigate prop to MainLayoutProps interface (after error: string)

    3. Add settings icon in header Flex (right side, left of login icon/Avatar, between News link and login):
       ```jsx
       {/* Settings icon - left of login icon */}
       <Tooltip label="设置" position="bottom-end" withArrow>
         <ActionIcon
           variant="subtle"
           size="md"
           radius="sm"
           onClick={() => onNavigate?.('/settings')}
           styles={{
             root: {
               backgroundColor: '#3f4b5c',
               color: '#a0a0a0',
               '&:hover': {
                 backgroundColor: '#4a576a',
               },
             },
           }}
         >
           <IconSettings size={18} />
         </ActionIcon>
       </Tooltip>
       ```

    4. In MainLayout usage in App.tsx, pass onNavigate prop:
       ```jsx
       <MainLayout
         onRefresh={handleRefresh}
         loading={loading}
         refreshing={refreshing}
         timeRange={timeRange}
         onTimeRangeChange={setTimeRange}
         repositoryCount={currentRepos.length}
         cacheTime={currentCacheTime}
         refreshMessage={refreshMessage}
         error={error}
         onNavigate={(path) => {
           // For now, just log - full navigation via React Router
           console.log('Navigate to:', path);
         }}
       >
         ...
       </MainLayout>
       ```

    5. For proper navigation, update App.tsx to use useNavigate from react-router-dom and pass it to MainLayout:
       - Add: const navigate = useNavigate()
       - Pass: onNavigate={(path) => navigate(path)}

    Follow existing pattern: Existing ActionIcon for GitHub login (line 322-342) shows styling pattern.
  </action>
  <verify>
    Run `npm run dev` in frontend directory to verify UI renders correctly.
    Click settings icon and verify it navigates to /settings route.
    Verify settings icon is positioned left of login icon/avatar.
    Check IconSettings is visible and matches login icon styling.
  </verify>
  <done>
    Settings icon with IconSettings added to MainLayout header.
    Settings icon positioned left of login icon/Avatar.
    Clicking settings icon navigates to /settings route.
    onNavigate callback integrated with React Router's navigate.
  </done>
</task>

</tasks>

<verification>
- [ ] Settings.tsx component compiles and exports Settings
- [ ] Settings page displays with "设置" title
- [ ] Slider has preset marks (0.5, 1, 4, 24 hours) with restrictToMarks
- [ ] Clone directory input has folder picker button
- [ ] Settings icon visible in header left of login icon
- [ ] Clicking settings icon navigates to /settings
- [ ] UpdateSettings called when slider value changes (auto-save)
- [ ] OpenDirectoryDialog backend method exists
- [ ] Folder picker opens native OS dialog
- [ ] Selected directory path updates clone directory input
- [ ] Settings persist across page refresh (reads from backend)
</verification>

<success_criteria>
Settings page is accessible via header icon and provides UI for configuring refresh interval and GitHub clone directory. All user changes are auto-saved to backend. Success notifications are silent per user decision, but errors display inline below input fields.
</success_criteria>

<output>
After completion, create `.planning/phases/01-user-settings/01-02-SUMMARY.md`
</output>
