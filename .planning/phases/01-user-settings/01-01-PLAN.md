---
phase: 01-user-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/config/app_config.go, backend/app.go]
autonomous: true
requirements: [PERS-01, PERS-02, API-01, API-02, API-03]
user_setup: []

must_haves:
  truths:
    - "Backend can read and return current settings (refresh interval, clone directory)"
    - "Backend accepts and validates settings updates"
    - "Backend persists settings to shared/app-config.toml"
    - "Backend reads refresh interval from config for refresh timing logic"
    - "Settings persist across application restarts"
  artifacts:
    - path: "backend/config/app_config.go"
      provides: "Config struct with RefreshInterval and GitHubCloneDir fields"
      exports: ["GetRefreshInterval", "GetGitHubCloneDir", "SetRefreshInterval", "SetGitHubCloneDir", "UpdatePreferences"]
      min_lines: 230
    - path: "backend/app.go"
      provides: "Settings API methods and refresh interval integration"
      exports: ["GetSettings", "UpdateSettings"]
      min_lines: 700
  key_links:
    - from: "backend/app.go"
      to: "backend/config/app_config.go"
      via: "appConfig field"
      pattern: "a\\.appConfig\\.(Get|Update)"
    - from: "backend/app.go"
      to: "shared/app-config.toml"
      via: "config.Save()"
      pattern: "config\\.(Save|Load)"
---

<objective>
Add backend configuration support for user settings (refresh interval, GitHub clone directory) with persistence and API methods for frontend access.

Purpose: Enable frontend to read and modify user settings that persist to config file and affect application behavior (refresh timing).

Output: Extended AppConfig struct with new fields, Settings API methods in App struct, refresh interval integrated into timing logic.
</objective>

<execution_context>
@C:/Users/bi_da/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bi_da/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-user-settings/01-CONTEXT.md
@backend/config/app_config.go
@backend/app.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend backend config with settings fields</name>
  <files>backend/config/app_config.go</files>
  <action>
    1. Add RefreshInterval float64 and GitHubCloneDir string fields to PreferencesConfig struct (line 34-38)
    2. Add GetRefreshInterval() and GetGitHubCloneDir() getter methods using RLock() for thread-safe reads
    3. Add SetRefreshInterval(float64) and SetGitHubCloneDir(string) setter methods using Lock() + Save()
    4. Add UpdatePreferences(Preferences) method that updates both fields atomically if changed, then calls Save()
    5. In Load() method (line 73-114), ensure defaults are set when config doesn't exist: RefreshInterval = 0.5, GitHubCloneDir = filepath.Join(os.UserHomeDir(), "github")

    Follow existing pattern: GetClientID() shows getter with RLock, SetGitHubConfig() shows setter with Lock and Save.
  </action>
  <verify>
    Run `go build` in backend directory to verify compilation succeeds.
    Check that default config file (if created) contains refresh_interval and github_clone_dir in [preferences] section.
  </verify>
  <done>
    PreferencesConfig struct has RefreshInterval and GitHubCloneDir fields.
    Getters return current values with RLock.
    Setters update and save with Lock.
    UpdatePreferences method exists.
    Default values: RefreshInterval = 0.5, GitHubCloneDir = platform-specific user home/github path.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create settings API methods in backend</name>
  <files>backend/app.go</files>
  <action>
    1. Create Settings struct to return to frontend:
       type Settings struct {
         RefreshInterval   float64 `json:"refresh_interval"`
         GitHubCloneDir    string  `json:"github_clone_dir"`
       }

    2. Add GetSettings() exported method that calls appConfig.GetRefreshInterval() and GetGitHubCloneDir(), returns Settings

    3. Add UpdateSettings(settings Settings) exported method that:
       - Validates refresh interval is between 0.1 and 24.0 hours, returns error if invalid
       - Validates clone directory is non-empty, returns error if invalid
       - Calls appConfig.UpdatePreferences(Preferences{RefreshInterval, GitHubCloneDir})
       - Returns nil on success

    4. In RefreshTrending() method (line 461-500), replace hardcoded time.Hour with appConfig.GetRefreshInterval()*time.Hour for the refresh check:
       if !a.lastRefresh.IsZero() && time.Since(a.lastRefresh) < time.Duration(a.appConfig.GetRefreshInterval()*3600)*time.Second

    5. In RefreshUserStarRepo() method (line 323-348), replace hardcoded time.Hour with same pattern

    6. In autoLoadInitialData() method (line 603-643), replace hardcoded time.Hour references with configurable interval

    Follow existing pattern: LoadTrendingDataResponse struct (line 270-274) shows response struct pattern.
  </action>
  <verify>
    Run `wails dev` to regenerate TypeScript bindings in frontend/wailsjs/go/backend/App.ts.
    Check that GetSettings and UpdateSettings are exported in generated bindings.
    Test with simple Go script: create App instance, call UpdateSettings with valid values, verify config file updates.
  </verify>
  <done>
    Settings struct exists with RefreshInterval and GitHubCloneDir fields.
    GetSettings() method returns current settings from config.
    UpdateSettings() validates and persists settings.
    Refresh timing uses configurable interval from config instead of hardcoded time.Hour.
    Wails bindings generated for GetSettings and UpdateSettings.
  </done>
</task>

</tasks>

<verification>
- [ ] backend/config/app_config.go compiles without errors
- [ ] backend/app.go compiles without errors
- [ ] GetSettings method exists in wailsjs bindings
- [ ] UpdateSettings method exists in wailsjs bindings
- [ ] Refresh interval defaults to 0.5 hours in config file
- [ ] Clone directory defaults to platform-specific path in config file
- [ ] UpdateSettings rejects invalid refresh intervals (<0.1 or >24)
- [ ] UpdateSettings rejects empty clone directory paths
- [ ] Refresh timing logic reads from config instead of hardcoded time.Hour
</verification>

<success_criteria>
Backend configuration system supports user settings for refresh interval and GitHub clone directory. Frontend can read current settings, update them with validation, and changes persist to shared/app-config.toml. Refresh interval configuration is applied to trending and starred repository refresh logic.
</success_criteria>

<output>
After completion, create `.planning/phases/01-user-settings/01-01-SUMMARY.md`
</output>
